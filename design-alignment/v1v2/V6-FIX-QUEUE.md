# V6 Fix Queue — Post-Primitive Reconciliation

Generated by PR #3R state-aware screenshot audit (2026-04-24).
Updated 2026-04-24 after PRs #3Q–#3AA landed.

## Status: V6 alignment complete for all screens with canonical mocks.

All P0 and P1 items below are DONE. See remaining items at bottom.

## Priority Guide
- **P0** = First impression or money-flow screen. Must fix before claiming V6 alignment.
- **P1** = Visible to most users in normal flow. Fix before launch.
- **P2** = Edge-case screens or minor layout issues. Fix post-launch if needed.

---

## P0 — Critical Screen-Level Fixes

### 1. Home page copy + layout (`/`)
**Mock:** `01-home.html`
**Current:** "You say a lot. / This time vow it." + live feed table + "Make your vow →" / "Dare a friend →"
**Mock:** "Make a vow. / Mean it." + "I vow to..." input field + verdict date pill + starter chips ("Gym 3x this week", etc.) + "Make my vow →"
**Drift:**
- Wrong headline copy
- Missing vow input field (the core interaction)
- Missing verdict date pill
- Missing starter chips (quick-start prompts)
- Live feed table doesn't exist in mock
- Missing "Sign in" nav link in header
- Missing hero label "YESTERDAY, YOU SAID TOMORROW."
- Logo/wordmark styling differs (current: gold square + uppercase text, mock: diamond seal + "Unbreakable *Vow*" mixed-style)
**Fix:** Full content rebuild of home page against mock. This is the first thing every user sees.
**PR slot:** #3T

### 2. Stake screen layout (`/stake`)
**Mock:** `03-pitch.html`
**Current:** Vertical form with section labels (DEADLINE, WITNESS, STAKE), ChoicePills for amounts, separate consequence card
**Mock:** Vow card with metadata (verdict date + judge), 4-column tile grid ($25/$50/$100/other with aspect-ratio squares), destination row ("If broken, goes to **NRA** change"), pill CTA ("Pick your judge — $50 →"), reassurance text
**Drift:**
- Completely different layout structure
- Missing vow card at top
- Tile grid is styled differently (current uses ChoicePills, mock uses square aspect-ratio tiles)
- CTA is pill variant with embedded amount + arrow-pill, not standard gold
- Missing "— Put weight on it —" centered section header
- Missing inline destination row
**Fix:** Full layout rebuild.
**PR slot:** #3S (already planned)

### 3. Quick-vow layout (`/quick-vow`)
**Mock:** `08-quick-vow.html`
**Current:** Vertical form with separate sections (input, deadline pills, witness field, stake row), standard GoldCTA
**Mock:** Card-based layout with vow card (input + verdict/stake/judge pills inside bordered container), "OR ONE-TAP" suggestion list with price+deadline columns, dual pill CTA row (gold "Make my vow — $50 →" + bordered "Dare a friend")
**Drift:**
- Completely different layout structure
- Missing "2 VOWS LIVE" header with avatar
- Missing one-tap suggestion list
- Missing dual CTA row (pill variant)
- Current layout is a form; mock layout is a power-user rapid-fire interface
**Fix:** Full layout rebuild.
**PR slot:** #3T

---

## P1 — User-Visible Drift

### 4. Dashboard layout (`/dashboard`)
**Mock:** `s20-dashboard-A-revised-v2.html`
**Current:** Greeting + vow cards with status pills + bottom CTA. Cards show status, vow text, stake info, witness name.
**Mock:** Similar structure but with key differences:
- Cards have "DAY X OF Y" countdown in top-right
- Cards show ON HOLD / UNTIL columns in metadata row
- "NEEDS YOU NOW" section with witnessing actions (verify button)
- "PENDING DARE" section at bottom
- Cards use ceremony variant styling (gold-line border)
**Drift:**
- Missing day countdown display
- Missing metadata columns (ON HOLD / UNTIL)
- Missing "NEEDS YOU NOW" witnessing section
- Missing "PENDING DARE" section
- Card styling: current uses dashboard variant, mock shows ceremony-style cards
**Fix:** Dashboard card template rebuild + add witnessing/dare sections.
**PR slot:** #3T

### 5. Witness landing (`/w/[token]`)
**Mock:** `09-witness-landing.html`
**Current:** Close to mock — headline, vow card, oath checkbox, CTA. Primitives render correctly (FrauncesH1 weight 400, GoldCTA correct gradient).
**Drift (minor):**
- Logo styling differs (current: "UNBREAKABLE VOW" eyebrow tag, mock: diamond + "Unbreakable *Vow*" wordmark)
- "Pass — I can't" secondary is currently a `MutedSecondary`-style text link, mock shows it as a centered text link (not a bordered button) — actually aligned
- Missing "First time? How this works →" footer link
- Mock shows "I'm in →" CTA without oath checkbox visible simultaneously (checkbox may be above fold)
**Fix:** Minor — add footer link, verify oath checkbox flow matches mock.
**PR slot:** #3T

### 6. Vow detail — active (`/vow/[id]`)
**Mock:** `14-active-countdown.html`
**Current:** Status pill + heading + subtitle + "Day X of Y" + witness dot + verdict date. Timeline section. Withdraw button.
**Mock:** Full countdown with DAYS/HRS/MIN/SEC grid, vow card with metadata (ON HOLD / GOES TO / JUDGE), "Text Nick" + "Share" action cards, ACTIVITY timeline with message entries.
**Drift:**
- Missing countdown timer grid (D/H/M/S)
- Missing "Text Nick" / "Share" action cards
- Missing activity/message timeline entries
- Vow card layout differs (current is simpler)
- Missing "VOW LIVE" header pill
**Fix:** Layout rebuild of active vow detail page.
**PR slot:** #3T

### 7. Vow detail — kept/broken (`/vow/[id]`)
**Current:** Clean layout with status pill, heading, vow card with metadata, timeline, CTAs. Primitives correct.
**Mock:** No exact 1:1 mock for vow detail kept/broken state (the m11/vow-broken mocks are for the standalone `/vow-kept` and `/vow-broken` pages, not `/vow/[id]`).
**Drift:** Minor — current implementation looks reasonable. May need polish pass for consistency with other vow detail states.
**PR slot:** #3T (low priority within P1)

### 8. History page (`/history`)
**Mock:** `s20-witnessing-all.html` (closest match — shows list of vow cards)
**Current:** Stats summary + filter pills + vow list cards with Stamp badges. RitualCard dashboard variant.
**Mock:** Tab header (Active 7 / Recent 12), section dividers (VERDICT DAY / ACTIVE), cards with avatar circles + status pills + stake amounts + time remaining.
**Drift:**
- Different header (current: FrauncesH1 "Your record." / mock: "Witnessing" with tabs)
- Different card layout (current: vow text + meta line + stamp / mock: avatar + name + vow text + status + amount)
- Missing section dividers
- Note: mock is for "Witnessing" page, not "History" — may be a different screen
**Fix:** History page may be intentionally different from the witnessing mock. Confirm which mock is canonical for /history before fixing.
**PR slot:** #3T (needs design decision)

### 9. Vow-kept standalone (`/vow-kept`)
**Mock:** `m11-vow-kept-charity.html`
**Current:** WaxSeal + "Crisis averted." + receipt card + GoldCTA + OutlinedGoldCTA. Primitives correct.
**Drift:**
- Heading copy: "Crisis averted." vs mock's "You actually did it."
- WaxSeal glyph: shield icon vs mock's trophy
- Missing "Sarah confirmed. Your word is gold." subtitle copy
- Receipt card: missing "THE RECEIPT" label, missing vow text in card, missing STREAK row
- CTA label: long wrapping text vs mock's "Share your win →"
- Secondary: "Make another vow →" vs mock's "Dare a friend"
**Fix:** Copy + layout alignment to mock.
**PR slot:** #3T

### 10. Vow-broken standalone (`/vow-broken`)
**Mock:** `vow-broken-charity.html`
**Current:** WaxSeal + BROKEN stamp + "Brutal. You broke it." + receipt card + GoldCTA
**Drift:**
- Extra "Brutal." in heading (mock just says "You broke it.")
- BROKEN stamp positioned above heading (mock: below receipt card)
- Missing "Sarah confirmed." subtitle
- Missing "THE RECEIPT" label in card
- Missing STREAK row in receipt
- CTA wraps to 2 lines
**Fix:** Copy + layout alignment to mock.
**PR slot:** #3T

---

## P2 — Edge Cases + Polish

### 11. Outcome page (`/outcome/[vowId]`)
**Mock:** `s19-outcome-resolved.html`
**Current:** Shows "Vow not found" — the server-side query can't find the vow (RLS issue or timing issue with test cleanup). Need to verify this works with real vow data.
**Fix:** Verify RLS allows public access to outcome page. Screenshot with persistent test data.
**PR slot:** #3T

### 12. Settings page (`/settings`)
**Mock:** No direct mock.
**Current:** FrauncesH1 "Settings." + "Not signed in" + GoldCTA "Sign in". When signed in, shows user info + settings cards.
**Fix:** No mock to compare against. Primitives render correctly. Skip unless mock is produced.
**PR slot:** N/A

### 13. Cast/dare page (`/cast`)
**Mock:** No direct mock.
**Current:** Form with FrauncesH1 "Dare a friend." + input fields + deadline radio cards + auth modal. Primitives correct.
**Fix:** No mock to compare against. Primitives render correctly. Skip unless mock is produced.
**PR slot:** N/A

### 14. Refine, Witness, Seal, Sent (`/refine`, `/witness`, `/seal`, `/sent`)
**Mocks:** `02-refine-nudge.html`, `05-witness-pick.html`, `05a-auth-pay-revised.html`, `05g-sealed-sent.html`
**Current:** Could not screenshot — these routes require VowFlowProvider state (in-memory, not database), which can't be injected via Playwright seed script.
**Fix:** These need manual smoke testing by walking through the actual creation flow in a browser. Compare visually to mocks during walkthrough.
**PR slot:** Manual smoke, then #3T for any drift found.

---

## Completion Log (2026-04-24)

| PR | Screen(s) | Status |
|----|-----------|--------|
| #3Q | Primitive reconciliation (all) | MERGED |
| #3S | `/stake` | MERGED |
| #3T | `/` home + `/refine` headline | MERGED |
| #3U | `/quick-vow` | MERGED |
| #3V | `/refine` bottom-sheet | MERGED |
| #3W | `/outcome/[vowId]` bug fix | MERGED |
| #3X | `/vow-kept` + `/vow-broken` | MERGED |
| #3Y | `/dashboard` polish | MERGED |
| #3Z | `/w/[token]` witness landing | MERGED |
| #3AA | `/vow/[id]` active state | MERGED |

---

## Remaining — Future Work

### /history — needs dedicated mock (deferred)
**Status:** Ship as-is. Current implementation works (filter pills, stats, Stamp badges). Primitives are V6-aligned after #3Q.
**Blocker:** No canonical HTML mock exists. Closest reference is `s20-witnessing-all.html` but that's for witnessing (other people's vows), not history (your own record).
**Action:** Create a dedicated /history mock in a future design pass, then rebuild to match.

### Flow-state screens — manual smoke needed
`/witness`, `/seal`, `/sent` require walking through the actual creation flow to verify. VowFlowProvider state is in-memory React context. Primitives are aligned but layouts haven't been screenshot-audited against mocks.

- [ ] `/witness` — compare to `05-witness-pick.html`
- [ ] `/seal` — compare to `05a-auth-pay-revised.html`
- [ ] `/sent` — compare to `05g-sealed-sent.html` or `05i-sealed-v6-sent.html`
